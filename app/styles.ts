import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  modal: {
    backgroundColor: '#fff',
    width: '75%',
    alignSelf: 'center',
    textAlign: 'center',
    justifyContent: 'flex-start',
    padding: 30,
    borderRadius: 4,
  },

});
